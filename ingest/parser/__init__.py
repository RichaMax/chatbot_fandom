from ingest_models import Page
from parser.client import FandomClient, HtmlResult


async def scrape_pages(html_results: list[HtmlResult]) -> list[Page]:
    pages = []

    for page_html_result in pages:
        pass

    return pages
